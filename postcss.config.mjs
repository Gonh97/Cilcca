/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 (via @tailwindcss/vite) configura automáticamente todos los
 * plugins de PostCSS necesarios — no hace falta incluir `tailwindcss` ni
 * `autoprefixer` aquí. Este archivo solo existe para plugins adicionales.
 */
export default {}
